# Loom run: S90 swap + crash-dummy occupants + multi-angle deformation fidelity

USER CLARIFICATION (2026-07-11): the reference images are not only "high speed" — they establish
MULTI-ANGLE deformation fidelity requirements: top view (small-overlap corner shear, side-impact
cave), underside (120 mph Fifth Gear), plus the speed tiers (100 mph = crush to A-pillar + doors
spring open; 120-200 mph = catastrophic). All three queued jobs run in this loom stream:
- **Stream A**: Mustang → Volvo S90 swap (foundation — deformation work lands on the new car).
- **Stream B**: occupants look like crash-test dummies (tan body, black/yellow calibration disks)
  and sit correctly IN the seats.
- **Stream C**: deformation fidelity tiers on the S90 — catastrophic high-speed + side/small-overlap
  + multi-angle correctness (top/underside/side reads).
- **Stream D** (added mid-run): bending/breaking physics for world materials — trees, 2x4s, plywood
  currently never fracture (structures topple via joints; members act as solid rigid bodies).
  Material-science slice determines real failure properties (wood MOR/MOE, plywood, masonry) scaled
  to engine impulse units; engine slice implements runtime fracture (snap mid-span, splinter,
  hinge-bend then separate). Condition: a 2x4 fence rail snaps when the car hits it at speed, a
  mid-size tree trunk SNAPS (not just uproots) above a threshold, plywood sheets split/shatter,
  each verified eyes-on + headless asserts; ordinary driving/parking taps break nothing.

## Goal (completion condition)
Done when ALL hold, each provable from surfaced output:
1. Game + crash lab load a Volvo S90 GLB in place of the Mustang, with Hood/DoorL/DoorR/Trunk panels, 4 wheels, and glass correctly mapped via a REGENERATED car-map.ts (not hand-faked). Check: `npm run build` exit 0; lab boots (`__LAB__.ready === true`).
2. Deformation density: S90 registered chassis-shell deformables ≥ 2× the Mustang's front-crush-zone vertex count (Mustang baseline: ~65k verts total / 46 meshes; front zone from dumpDeformables). Check: `__LAB__.dumpDeformables()` sums.
3. Physics geometry derives from the S90 (wheelbase/half-width/nose overhang/panel boxes from the new car-map; hardcoded Mustang values like protocols.ts CAR_HALF_WIDTH_M=0.97 updated). Check: grep + sim suite green.
4. Regression: full sim suite passes (233 tests baseline; band updates only with measured justification recorded here) + verify/crash-lab.mjs all assertions pass.
5. Eyes-on: NHTSA-56 post-crash screenshots on the S90 show structural crush + hood tent, car renders correctly (paint/glass/wheels aligned). Orchestrator-eyes verified vs crash-reference-set.
Constraints: damage-model semantics preserved; token-conservative tiering (Haiku scans / Sonnet drafts / Fable orchestrates+verdicts only); no commits without user ask.
Bound: stop after 3 gate passes OR on a hard asset blocker (unusable mesh/licensing/format) — report honestly.

## Stream B condition (dummies)
Occupant ragdolls render as crash-test dummies (tan/orange body segments, black-yellow quadrant
calibration disks on head/knees, articulated look) AND all 4 spawn seated in the S90's actual seat
positions (hips on cushion, feet in footwell, head below roofline — verified by eyes-on interior
screenshot + seat-anchor asserts). Existing occupant physics (injury model, ejection) unchanged —
sim tests green.

## Stream C condition (deformation fidelity)
Per-reference eyes-on match, each angle screenshot-verified:
1. 100 mph (160 km/h) frontal: crush reaches A-pillar, hood torn, doors SPRING OPEN (latch fails,
   door rotates open but may stay on hinge), windshield shattered.
2. 120+ mph: catastrophic — cabin collapse begins, wheels torn, car may leave ground/end off-wheels.
3. IIHS small overlap (top view): corner shear outboard of rails, struck wheel torn/displaced,
   door skin peeled, intact side pristine.
4. Side MDB/pole (top view): deep door cave-in profile matching reference, roof buckle over strike.
5. Underside view stays coherent (no inverted/spiked geometry) at every tier.
Bound: 3 gate passes per stream.

## Phases
- **P0 scout (parallel)**: A) Blender headless inventory of the .blend; B) codebase Mustang-coupling
  inventory; C) occupants-feature inventory (visuals + seating anchors).
- **P1 asset pipeline (A)**: Blender script → optimized GLB, node naming per car-map conventions,
  crush-zone tessellation preserved/increased, interior decimated, textures packed.
- **P2 integration (A)**: GLB swap, car-map regen (scripts/analyze-car.mjs), tuning/protocol
  constants, occupant seat anchors. ∥ **P2b (B)**: dummy visual build (independent of swap).
- **P3 (A/B)**: recalibration + dummy seating on S90; sim suite + crash-lab bands retuned measured.
- **P4 (C)**: deformation tiers on the S90 (door-latch failure model, A-pillar crush tier,
  catastrophic tier, side/small-overlap fidelity) — own gate loop per condition item.
- **P5 gate**: loom:verifier adversarial PASS/FAIL vs ALL stream conditions + orchestrator eyes-on.

## RESUMED (2026-07-11 evening)
- D eyes-on battery: COMPLETE + orchestrator-verified (fence rails snapped into pieces ✓ eyes-on;
  shed plywood debris field ✓ eyes-on; tree crown felled w/ honest caveat re stump framing; body
  deltas +3/+25/+1). New hook __GAME__.boostTo(speedKmh); teleport-phantom-velocity landmine
  documented in the hook comment. Stream D CLOSED (task #7).
- S90 integration: in flight; swap observed LIVE in the battery's screenshots (HUD "Volvo S90",
  6 panel chips). NEW DEFECT observed by orchestrator: white speckled patches on hood/roof/trunk
  (normals corruption or z-fighting on split panels) — integration worker's S7 clause covers it;
  verify addressed in its report, else bounce back.
- QUEUED next: task #9 coherent crease noise (grocery-bag wrinkle fix) immediately after
  integration lands.

## WRAP-UP (2026-07-11, session end — user request)
Tree state at close: `npm run build` clean; **241/241 tests green (83 files)**; preview on :4173 live;
working tree UNCOMMITTED (includes realism pass + dummy reskin + fracture engine + S90 GLB).
- **DONE + verified**: NHTSA-56 realism pass (structural crush visual, hood tent, crush-gated hood
  break, lab guide-release + Hide-barrier, diagnostics) — 19/19 harness + eyes-on. Dummy reskin —
  eyes-on accepted. Fracture engine (trees snap, rails/studs/plywood/drywall fracture with material-
  anchored thresholds, fragments, debris-mass fix) — 8 new tests, full suite green, perf 0.7ms chaos.
  S90 GLB exported + orchestrator-validated (density ~5x Mustang panels).
- **IN PROGRESS**: Stream D eyes-on screenshot battery (implementation proven headlessly; the
  in-world fracture screenshots were never captured — worker kept failing to ram the tree at speed).
  Stream B seating (waits on S90).
- **NOT STARTED**: P2 S90 integration (CAR_CONFIGS entry — needs wheel-group/glass-material check
  in the GLB — GLB swap, PanelKey 6-panel extension + entity-ID renumbering + HUD/test silent traps
  per p0b docs), P3 recalibration + dummy seating, Stream C deformation tiers (100-200mph, side/
  small-overlap multi-angle), P5 independent gate.
- **RESUME POINTERS**: this file + p0a JSON (scratchpad s90-inspect/) + p0b/p0b2/p0c/p0d/d1 docs in
  docs/loom/. Next session's first slices: (1) Stream D eyes-on battery; (2) P2 integration.

## Slice status / cost ledger
| Slice | Tier | Status | Tokens |
|---|---|---|---|
| P0-A blend inventory | sonnet | DONE → scratchpad s90-inspect/s90-inventory.json | 68k |
|   ↳ key facts: 139 flat meshes, 1:1m scale (5.00×2.01×1.43), 4-DOOR (Door Front/Rear are single X-MIRRORED meshes — must apply mirror + split L/R), panels are low-vert Subsurf cages (hood 440 raw → subsurf L2 gives ~16x density: we control deformation density per part), NO ENGINE modeled (engine bay empty under tented hood — needs filler), brake discs/tires absurdly dense (24k/18k per corner → decimate), trailing-space + "Windsheild" misspelled names (trim/rename on export), 547MB referenced textures (compress hard), 0 missing textures. |
| P0-B coupling inventory | sonnet | dispatched | — |
| P0-C occupants inventory | haiku | DONE → docs/loom/p0c-occupants-inventory.md | 80k |
| P0-D destructible-world + engine fracture capability inventory | sonnet | dispatched | — |
| P0-B2 sim/verify coupling slice | external session (contributed) | DONE → docs/loom/p0b2-sim-verify-couplings.md | n/a |
| P0-D destructibles inventory | sonnet | DONE → docs/loom/p0d-destructibles-inventory.md (no body ever splits; engine capable; reuse yield→break pattern; setForeignMass gap found) | 232k |
| D1 fracture material spec | sonnet | dispatched (anchored to play-validated tree/shed thresholds) | — |
| B1 dummy visual reskin (look only, seating later) | sonnet | dispatched; CORRECTION SENT (dummySkin.ts document.createElement breaks headless sim → lazy/guarded texture + full suite green required) | — |
| P0-B coupling inventory | sonnet | DONE → docs/loom/p0b-mustang-coupling.md (545 lines, 5-way cross-validated). DECISION (orchestrator): rear doors = full detachable panels (doorRL/doorRR join PanelKey); glass entity IDs renumber; silent traps (HUD PANEL_ORDER arrays, ['doorL','doorR'] test literals) on the P2 checklist | 268k |
| D1 fracture material spec | sonnet | DONE → docs/loom/d1-fracture-material-spec.md. Headline: rails/studs/plywood/drywall fracture BEFORE weld-pop → fracture engine runs parallel to yield machine. Risks logged (force/torque co-scaling approx; sheet break-plane needs hit-point signal — reuse stepDamageSystem's snapshotted hits; re-validate weld-pop-primary tests) | 152k |
| P1 S90 Blender export pipeline | sonnet | worker killed by connection drops; GLB DELIVERED + ORCHESTRATOR-VALIDATED (blender re-import dump): 26.8MB, 127 objects, 377,054 verts, dims 5.00×2.01×1.43 ✓, no trailing-space names ✓, Hood 8,401 / DoorL 9,615 / DoorR 9,613 / DoorRL 9,386 / DoorRR 9,399 / Trunk 5,984 verts (all ≥ budget; Mustang hood was ~1.7k → ~5x density), EngineBlock filler present, Windshield/RearWindow renamed correctly. REMAINING (deferred until D2 lands — analyze-car writes shared car-map.ts): CAR_CONFIGS entry check + analyze-car run + car-map-s90-snapshot.ts + confirm tire/door-glass node names | ~350k (est, incl. killed passes) |
| B1 dummy reskin RESULT | sonnet | DONE + ORCHESTRATOR-EYES ACCEPTED (tan Hybrid-III read, chest/knee calibration disks crisp, ejected ragdoll reads as dummy; 233/233 full suite; DOM-guarded for headless). Files: occupants/dummySkin.ts (new) + visuals.ts. Remaining stream B: seating on S90 (P3) | 268k |
| D2 fracture implementation | sonnet | dispatched (parallel fracture check alongside yield machines; fragments replace parent; setForeignMass fixes; new fracture-materials test; eyes-on battery) | — |
